{
  "customEnvironment": "vsr",
  "registries": ["npm", "vsr"],
  "teamId": "team_I0bmFHPp3qnoaSnAA6xP70tU",
  "projectSettings": {
    "installCommand": "npm install --timing --loglevel=http --no-audit --no-fund --no-update-notifier --force --ignore-scripts",
    "buildCommand": "mkdir dist && echo \"hello world\" > dist/index.html",
    "outputDirectory": "dist",
    "framework": null
  }
}
